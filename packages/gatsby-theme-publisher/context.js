import React from 'react';

const ThemeOptions = './src/util/ThemeOptions';

// eslint-disable-next-line import/prefer-default-export
export const PublisherOptions = React.createContext(ThemeOptions);
